/**
* Sex enumeration
*
* Used to represent the sex of the NPC
* Stored in DB as string for ease of use, but stored as an enum in code for safety
* @author  Scott
* @version 0.1
* @since   2023-01-15
* @status Done
*/

package com.scottfarsace.spring.npcgenerator.models;

public enum Sex
{
    Male,
    Female
}